import java.util.*;

public class SearchPriorityQueue {

    // Fields
    // -----------------------------------------------------------------------------
    public LinkedList<SearchTreeNode> list;
    private SearchTreeNode head = new SearchTreeNode( null, null, null, 0);

    public SearchPriorityQueue() {
        list = new LinkedList<>();

    }

    public boolean add( SearchTreeNode s ) {

    }
}
